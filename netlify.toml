# =============================================
# CONFIGURATION NETLIFY - E-Réussite
# =============================================

[build]
  command = "npm run build"
  publish = "dist"
  
[build.environment]
  NODE_VERSION = "18"

# =============================================
# SECRETS SCANNING - Exclude documentation files
# =============================================
[build.processing.secrets]
  enabled = true
  
[build.processing.secrets.omit_paths]
  paths = [
    "*.md",
    "**/*.md",
    "public/check-quiz-options.html",
    "public/test-quiz-data.html"
  ]

# =============================================
# FUNCTIONS DIRECTORY
# =============================================
[functions]
  directory = "netlify/functions"
  
# =============================================
# REDIRECTS (SPA React Router)
# =============================================
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# =============================================
# HEADERS (Sécurité)
# =============================================
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "strict-origin-when-cross-origin"
